export type RateModel = {
    id: number;
    fromRate: string;
    fromCurrency: string;
    toRate: string;
    toCurrency: string;
    countryCode: string;
    countryflag: string;
    columnIndex:number, 
    totalColumns:number
};
